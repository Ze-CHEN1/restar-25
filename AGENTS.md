# AGENTS.md：RoboMaster 视觉研究工作区

## 工作区目标

这是同济大学 SuperPower 战队 2025 赛季视觉代码，以及一套用于长期、可恢复、可审计研究的 Codex 工作区。Agent 的职责是帮助研究者形成问题、建立基线、提出假设、实现受控改动、执行离线实验、分析失败并保存下一步，而不是用一次对话代替工程验证。

默认使用中文沟通；代码标识符、命令和已有技术术语保持项目原有风格。

## 开始任务前必须读取的细则

根文件只保留全局规则。开始下列工作前，必须完整读取对应文件：

- 算法研究、论文/资料核查、方案比较、误差分析：`agents/instructions/vision-research.md`
- 修改 C++、CMake、YAML、脚本或接口：`agents/instructions/implementation.md`
- 数据集实验、录像回放、基准测试、标定、台架或实车验收：`agents/instructions/experiments-and-validation.md`

任务匹配多个类别时，读取所有对应文件。首次实质进度更新应列出本轮读取的细则路径。细则与本文件冲突时，以本文件为准；更深目录若存在 `AGENTS.md` 或 `AGENTS.override.md`，按 Codex 的就近覆盖规则执行。

## 系统边界与模块地图

- `io/`：相机、IMU、串口、CAN、ROS2 接口。这里的改动可能影响硬件和通信安全。
- `tasks/auto_aim/`：装甲检测、分类、PnP/坐标解算、整车跟踪、瞄准、开火与轨迹规划。
- `tasks/auto_buff/`：能量机关检测、解算、预测与瞄准。
- `tasks/omniperception/`：全向感知与决策。
- `calibration/`：相机内参、手眼和 robot-world-hand-eye 标定。
- `tools/`：日志、滤波、弹道、图像、录制和通用工具。
- `src/`：不同兵种及调试程序的系统装配入口。
- `tests/`：模块、离线回放和硬件测试程序。
- `configs/`：机器人、相机、通信和算法参数。
- `assets/`：模型、测试视频和演示资源；二进制模型不是普通源码。
- `research/tasks/`：研究者维护的正式任务输入。
- `research/projects/`：Agent 持久化的研究状态、实验记录和产物。

改变跨模块数据语义时，必须从采集时间戳一路检查到最终命令：相机/姿态对齐 → 检测 → 解算 → 跟踪/预测 → 规划/开火 → 通信。

## 标准研究闭环

每个研究任务都遵循以下顺序：

1. 写清目标、基线、数据范围、指标、约束和验收门槛。
2. 先复现基线；无法复现时记录精确阻塞，不伪造比较结果。
3. 至少登记两个机制不同的候选方案，再选择信息增益最高的一条。
4. 先做最小可证伪实验；失败也要保存原因和可复用结论。
5. 只有在任务配置允许时才修改工作区源码，并保持改动小而可回退。
6. 按“静态检查 → 单元/模块测试 → 离线录像回放 → 台架 → 实车”逐级验证。
7. 同步项目的 `progress.md`、`hypothesis-ledger.md`、`experiment-ledger.md`、`decisions.md` 和 `README.md`。

一个回合必须有明确边界。没有完整解决时，仍需留下最强结果、失败位置和下一项可执行动作，让下一次运行不依赖旧聊天继续。

## 证据等级

所有性能或正确性结论必须标明以下最高证据等级之一：

1. `hypothesis`：只有机制推测。
2. `code-reviewed`：实现和接口经过静态审查，尚无运行证据。
3. `offline-reproduced`：在固定数据、配置和命令上可复现。
4. `benchmark-supported`：相对冻结基线达到预先声明的指标门槛。
5. `hardware-validated`：研究者确认已通过台架或实车验证。
6. `competition-validated`：研究者确认在比赛条件下得到验证。

Agent 可以把证据提升到 `benchmark-supported`，但不能自行写成 `hardware-validated` 或 `competition-validated`。单段视频、挑选样本、主观观感和一次成功运行不能证明总体性能提升。

## 数据与实验纪律

- 每个实验记录代码提交、配置、数据集或录像标识、命令、随机种子、硬件/系统环境、指标和原始产物路径。
- 训练、调参和报告数据必须分离；同一数据被反复用于决策时，不再把它当作独立测试集。
- 同时报告准确性和实时性。延迟优先给出中位数、P95/P99、抖动和丢帧，而不只给平均 FPS。
- 分开统计距离、转速、装甲板切换、遮挡、曝光、敌方颜色和机器人类型等关键场景。
- 配置单位必须明确。角度/弧度、毫秒/秒、相机坐标/云台坐标/世界坐标不得靠猜测转换。
- 不提交未经许可的数据集、比赛私有录像、个人信息、机器日志或大体积实验产物。

## 安全与权限边界

- 不读取、写入或提交 token、cookie、私钥、`.env`、登录目录和设备凭据。
- 不自动操作真实相机、串口、CAN、云台、摩擦轮、拨弹机构或 ROS2 机器人节点。
- 不自动运行 `autostart.sh`，不安装系统包，不写 udev 规则，不改变设备权限。
- 任何可能造成云台运动或发射的测试必须停下来，请研究者确认场地隔离、急停、限位、供弹状态和现场监护。
- 未获本轮明确授权，不提交或推送 Git，不发布结果，不修改远端资源。
- 不覆盖研究者已有改动，不使用破坏性 Git 或文件清理命令。
- 模型权重和二进制 SDK 仅在明确要求且来源、许可、哈希和回退方案清楚时更换。

## 代码与版本控制

- 保持 C++17、现有命名空间和模块边界；不为单个实验引入无关框架。
- 公共行为变化必须同步配置示例、调用方和文档。新增 YAML 参数需给出默认值、单位、合法范围与兼容策略。
- 避免在热路径反复分配、阻塞 I/O 或无界日志。时间戳使用单调时钟参与时差计算。
- 修复应包含能暴露原问题的测试或可复现实验；做不到时说明原因。
- 修改前后检查 `git status` 和 `git diff`，只处理本任务相关文件。
- 构建基线命令为 `cmake -B build && cmake --build build -j"$(nproc)"`；若环境缺少 OpenVINO、Ceres、相机 SDK 或硬件，执行可用的较低层级检查并明确缺口。

## Agent 队列状态

合法状态为：`draft`、`queued`、`researching`、`needs-data`、`needs-hardware-validation`、`needs-human-review`、`blocked`、`candidate-complete`、`accepted`、`archived`、`runtime-error`。

- 任务输入有歧义、数据许可不清或验收指标互相冲突时，使用 `needs-human-review`。
- 缺少决定性数据时使用 `needs-data`；缺少台架/实车确认时使用 `needs-hardware-validation`。
- 仍有具体可执行下一步时保持 `researching`，不要轻易标记 `blocked`。
- 达到任务中全部离线验收门槛后才可标记 `candidate-complete`，等待研究者确认。
- 只有研究者可以设置 `accepted`。

## 完成标准

结束任务时报告：实际改动和路径、采用的假设、运行过的命令与结果、证据等级、尚未覆盖的场景或风险、下一项最有价值的动作。不能把“代码写完”或“程序成功退出”单独视为研究完成。
